{
  "id": "malformed-frontmatter",
  "fixture": "malformed-frontmatter",
  "evaluations": [
    {
      "id": "audit-hub",
      "command": ["audit", "hub"],
      "result_file": ".hub/reports/latest-audit.json",
      "match": "diagnostic_subset",
      "expected_diagnostics": [
        {
          "code": "malformed_frontmatter",
          "severity": "error",
          "target": ".hub/issues/malformed.md",
          "message": "Issue frontmatter is malformed.",
          "recommendation": "Repair the YAML frontmatter with the deterministic issue update command or recreate the issue file."
        }
      ]
    }
  ]
}
