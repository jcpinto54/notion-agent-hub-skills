# Tasks

## Dependency Graph

`ui-shell` should depend on `api-client`, but `ui-shell` is not listed in `change.yml`.

## Issues

- [ ] api-client - Build API client
- [ ] ui-shell - Build UI shell

## Parallelization Notes

Tasks are not independently claimable until links are fixed.

## Blockers

Missing issue file for `missing-contract`.
