version: 3
source_of_truth: file
project: "Valid Lite Hub"

cli:
  strict_writes: true
  preserve_unknown_frontmatter: true

agents:
  enabled: false
  runner: codex
  command: codex
  require_subagent_for_tasks: true

audit:
  tdd_required_for_implementation: true
  e2e_required_for_ui: true
  stale_claim_minutes: 120

dashboard:
  enabled: false
  mode: read-only
