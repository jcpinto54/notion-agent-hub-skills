interface:
  display_name: "List Agent Hub Issues"
  short_description: "Show eligible and blocked Agent Hub work"
  default_prompt: "Use $list-agent-hub-issues to show eligible, in-review, and blocked Agent Hub issues without modifying or spawning work."
